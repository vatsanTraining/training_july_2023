server:
  port: 6070

spring:
  application:
    name: service-two  
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true 
    service-url:
      defaultZone: http://localhost:8761/eureka
        